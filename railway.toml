[build]
builder = "nixpacks"

[deploy]
startCommand = "java -Xmx512m -Xms256m -Dserver.port=$PORT -jar target/*.jar"
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
