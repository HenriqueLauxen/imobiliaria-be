spring.application.name=imobiliaria
server.port=${PORT:8080}

# Configuração do banco de dados PostgreSQL via variáveis de ambiente
spring.datasource.url=${DATABASE_URL:jdbc:postgresql://db.aftpdkghgfepsqmvtneo.supabase.co:5432/postgres?sslmode=require}
spring.datasource.username=${DATABASE_USERNAME:postgres}
spring.datasource.password=${DATABASE_PASSWORD:Hxen@!20250}
spring.datasource.driver-class-name=org.postgresql.Driver

spring.datasource.hikari.maximum-pool-size=${HIKARI_MAX_POOL_SIZE:5}
spring.datasource.hikari.minimum-idle=${HIKARI_MIN_IDLE:2}
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.idle-timeout=600000
spring.datasource.hikari.max-lifetime=1800000
spring.datasource.hikari.keepalive-time=300000
spring.datasource.hikari.validation-timeout=5000
spring.datasource.hikari.leak-detection-threshold=60000

# JPA/Hibernate
spring.jpa.show-sql=${SHOW_SQL:false}
spring.jpa.hibernate.ddl-auto=${DDL_AUTO:update}
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true
spring.jpa.open-in-view=false

spring.jpa.hibernate.naming.physical-strategy=org.hibernate.boot.model.naming.CamelCaseToUnderscoresNamingStrategy

spring.jpa.properties.hibernate.jdbc.time_zone=America/Sao_Paulo

# Configuração do Supabase Storage para as fotos
supabase.url=${SUPABASE_URL:https://aftpdkghgfepsqmvtneo.supabase.co}
supabase.key=${SUPABASE_KEY:sb_secret_GB-2FmUor9OdJxNCc8Jstw_SY_0cDGM}
supabase.bucket=${SUPABASE_BUCKET:imoveis}

# Tamanho máximo para upload
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB